from django.apps import AppConfig

class ClientMapLayersConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "client_map_layers"
    verbose_name = "Couches cartographiques client"
